#ifndef ASCIILOFI_H
#define ASCIILOFI_H
#include <iostream>
#include <vector>
#include <string>

void play_video(std::string* video_name, int* width, int* height);
void browse_and_select(std::vector<std::string>* videos);

#endif